<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Captures - <%= userName %></title>
  <link rel="stylesheet" href="/style.css"/>
</head>
<body>
  <div class="container">
    <h2>Captures for <%= userName %></h2>
    <div class="grid">
      <% images.forEach(u => { %>
        <img src="<%= u %>" alt="capture"/>
      <% }) %>
    </div>
  </div>
</body>
</html>
